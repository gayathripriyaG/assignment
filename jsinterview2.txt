1.Give an example where call apply bind is. required?
ans: If we want to use functions or access them from other methods then we call,apply and bind.
     call and apply invoke the immediately but with bind we can invoke the function later.

2.What is the difference between readFile and readFileSync?
ans:readFileSync() is synchronous and blocks execution until finished. These return their results
    as return values. readFile() are asynchronous and return immediately while they function in the
    background. You pass a callback function which gets called when they finish. 

3.What does process in node.js mean?
ans:The process object in Node. js is a global object that can be accessed inside any module without requiring it.

4.Explain what node.js is?
ans:Node. js is a JavaScript runtime environment that achieves low latency and high throughput by taking a “non-blocking”
 approach to serving requests. It is used for executing JavaScript code server-side.

5.What is the difference of JS from browser to JS on node.js?
ans:
Node:
In Node everything is a module. You must keep your code inside a module.
Node processes request object.
Browser:
Moduling is not mandatory in client side JavaScript, i.e. in browsers.
Browsers processes response objects.

6.Write three different ways to reverse a string in Javascript? a. using inbuilt method, b. iteratively, c. recursively
ans: a.str.split("").reverse().join("")
b.
let newstr = ""
for(var i = str.length-1;i>=0;i--){
   newstr += str[i]
}
console.log(newstr)
   
7.Write a program to check two objects are equal ( deep equal )?
ans: JSON.stringify(obj1) === JSON.stringify(obj2);

8.What is shallow equal?
ans: Two values have shallow equality when all of their members
    are strictly equal(===) to the corresponding member of the other.